apiVersion: v1
kind: Service
metadata:
  name: engine-service
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: engine
