apiVersion: apps/v1
kind: Deployment
metadata:
  name: face-detection
  labels:
    app: face-detection
spec:
  replicas: 1
  selector:
    matchLabels:
      app: face-detection
  template:
    metadata:
      labels:
        app: face-detection
    spec:
      containers:
      - name: face-detection
        #image: registry.forge.hefr.ch/mse_pi/services/face_detection:latest
        image: IMAGE_NAME
        imagePullPolicy: Always
        ports:
        - containerPort: 80 
        env:
        - name: APP_HOST
          value: 0.0.0.0
        - name: APP_PORT
          value: "80"
        - name: APP_LOG
          value: info
        - name: APP_ENGINE
          value: "http://service-engine.project-dev.svc.cluster.local/notify"
      imagePullSecrets:
      - name: mysecretdeployer
