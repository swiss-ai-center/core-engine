apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: core-engine-ingress
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "16m"
    nginx.org/client-max-body-size: "16m"
spec:
  rules:
    - host: core-engine-swiss-ai-center.kube.isc.heia-fr.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: core-engine-service
                port:
                  number: 80
  tls:
    - hosts:
        - core-engine-swiss-ai-center.kube.isc.heia-fr.ch
